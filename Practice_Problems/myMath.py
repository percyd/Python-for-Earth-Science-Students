#import numpy as np
def circleArea(myradius):
    """
    return the area of a circle, given a radius as input
    """
    if myradius is None:
        pass
    else:
        return  np.pi * myradius**2
        #return myarea  
